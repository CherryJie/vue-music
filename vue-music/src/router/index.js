import Vue from 'vue';
import Router from 'vue-router';

// 注册
Vue.use(Router);

export default new Router({
  routes: [
    {
      path: '/',
      // name: 'HelloWorld',
      // component: HelloWorld,
    },
  ],
});
